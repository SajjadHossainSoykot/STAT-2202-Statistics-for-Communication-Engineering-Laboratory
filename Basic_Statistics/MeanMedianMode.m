%Data
data = [ 15,9,26,13,14,12,22,19];

%Mean
mean_val = mean(data);

%Median
median_val = median(data);

%Mode
mode_val = mode(data);

%Display Results
fprintf('Mean:%.2f\n',mean_val );
fprintf('Median:%.2f\n',median_val  );
fprintf('Mode:%.2f\n',mode_val );
